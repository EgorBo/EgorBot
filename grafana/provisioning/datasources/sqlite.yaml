apiVersion: 1

datasources:
  - name: EgorBot SQLite
    uid: egorbot-sqlite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /opt/egorbot/egorbot.db
    editable: false
